<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:animateLayoutChanges="true">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recentCallsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:paddingStart="@dimen/sbdv_recent_calls_padding"
        android:scrollbars="none"
        tools:itemCount="1"
        tools:listitem="@layout/sbdv_recent_call" />

    <include
        android:id="@+id/emptyContactsCallsLayout"
        layout="@layout/sbdv_empty_contacts_layout"
        android:visibility="gone" />

</FrameLayout>
